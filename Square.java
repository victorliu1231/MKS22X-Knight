public class Square extends KnightBoard{
  int r;
  int c;
  int numJumpSpots;

  public Square(int r, int c){
    if (r == 0 && c == 0 ||
        r == 0 && c == board[r].length-1 ||
        r == board.length-1 && c == 0 ||
        r == board.le
        )
  }
}
